# =============================================================================
# CORE METADATA
# =============================================================================
schema_version: "1.2.0"
name: "iam-passrole+ecs-runtask"
title: "ECS Run Task to Admin"
description: "User with iam:PassRole and ecs:RunTask can override existing task definition commands to escalate to admin via Fargate"
cost_estimate: "$0/mo"
pathfinding-cloud-id: "ecs-008"

# =============================================================================
# CLASSIFICATION
# =============================================================================
category: "Privilege Escalation"
sub_category: "new-passrole"
path_type: "one-hop"
target: "to-admin"
environments:
  - "prod"

# =============================================================================
# ATTACK PATH
# =============================================================================
attack_path:
  principals:
    - "arn:aws:iam::{account_id}:user/pl-prod-ecs-008-to-admin-starting-user"
    - "arn:aws:iam::{account_id}:role/pl-prod-ecs-008-to-admin-target-role"

  summary: "starting_user → (ecs:RunTask with command override on existing task definition, passing admin role) → ECS Fargate task attaches admin policy to starting user → admin access"

# =============================================================================
# PERMISSIONS
# =============================================================================
permissions:
  required:
    - principal: "pl-prod-ecs-008-to-admin-starting-user"
      principal_type: "user"
      permissions:
        - permission: "iam:PassRole"
          resource: "arn:aws:iam::*:role/pl-prod-ecs-008-to-admin-target-role, arn:aws:iam::*:role/pl-prod-ecs-008-to-admin-execution-role"
        - permission: "ecs:RunTask"
          resource: "*"

  helpful:
    - principal: "pl-prod-ecs-008-to-admin-starting-user"
      principal_type: "user"
      permissions:
        - permission: "ecs:ListTaskDefinitions"
          purpose: "Discover existing task definitions to exploit"
        - permission: "ecs:DescribeTasks"
          purpose: "Monitor task execution status and verify task completion"
        - permission: "ecs:ListClusters"
          purpose: "Discover available ECS clusters"
        - permission: "ecs:StopTask"
          purpose: "Stop running tasks during cleanup"
        - permission: "ec2:DescribeVpcs"
          purpose: "Find default VPC for Fargate task network configuration"
        - permission: "ec2:DescribeSubnets"
          purpose: "Find subnet in default VPC for Fargate task network configuration"
        - permission: "iam:DetachUserPolicy"
          purpose: "Remove admin policy from starting user during cleanup"
        - permission: "iam:ListAttachedUserPolicies"
          purpose: "Verify privilege escalation success by listing attached policies"
# =============================================================================
# MITRE ATT&CK
# =============================================================================
mitre_attack:
  tactics:
    - "TA0004 - Privilege Escalation"
    - "TA0002 - Execution"
  techniques:
    - "T1078.004 - Valid Accounts: Cloud Accounts"
    - "T1610 - Deploy Container"

# =============================================================================
# TERRAFORM
# =============================================================================
terraform:
  variable_name: "enable_single_account_privesc_one_hop_to_admin_ecs_008_iam_passrole_ecs_runtask"
  module_path: "modules/scenarios/single-account/privesc-one-hop/to-admin/ecs-008-iam-passrole+ecs-runtask"
